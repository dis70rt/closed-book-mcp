[project]
name = "mcp-closed-book"
version = "1.0.0"
description = "A fast, strictly closed-book local RAG MCP server for exam preparation using Hybrid Search."
authors = [
    { name = "dis70rt", email = "dis70rt@example.com" }
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faiss-cpu>=1.15.0",
    "mcp>=2.2.0",
    "numpy>=2.5.3",
    "pymupdf>=1.28.2",
    "pytest>=9.1.1",
    "rank-bm25>=0.2.2",
    "sentence-transformers>=6.0.1",
]
